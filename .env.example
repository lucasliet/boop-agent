# ---- Convex ----
# Get these by running: npx convex dev
# Convex will create a project and auto-populate your .env.local.
CONVEX_DEPLOYMENT=
CONVEX_URL=
VITE_CONVEX_URL=

# ---- Sendblue (iMessage bridge) ----
# Sign up at https://sendblue.co, get your API keys + provisioned number.
# Webhook URL you'll set in Sendblue dashboard:  <PUBLIC_URL>/sendblue/webhook
#
# SENDBLUE_FROM_NUMBER MUST be your Sendblue-provisioned number (the one
# people text TO), not your personal cell. Sendblue's API requires it.
SENDBLUE_API_KEY=
SENDBLUE_API_SECRET=
SENDBLUE_FROM_NUMBER=

# ---- Claude model ----
# Uses your Claude Code subscription automatically — no separate API key needed.
# Override with ANTHROPIC_API_KEY if you want to bypass the subscription.
BOOP_MODEL=claude-sonnet-4-6
# ANTHROPIC_API_KEY=

# ---- Embeddings (optional — unlocks vector search for memory recall) ----
# Pick one. If neither is set, recall falls back to substring search.
# VOYAGE_API_KEY=
# OPENAI_API_KEY=

# ---- Server ----
PORT=3456
# Public URL used for Sendblue webhooks + OAuth redirect URIs.
# Local dev: use ngrok, e.g. https://abc123.ngrok.app
PUBLIC_URL=http://localhost:3456

# ---- Integrations — OAuth (for the debug dashboard's "Connect" flow) ----
# Register redirect URI in each provider's console: <PUBLIC_URL>/oauth/<provider>/callback
# GOOGLE_CLIENT_ID=
# GOOGLE_CLIENT_SECRET=
# SLACK_CLIENT_ID=
# SLACK_CLIENT_SECRET=

# ---- Integrations — static tokens (fastest path for personal use) ----
# Google (Calendar + Gmail)
#   Easiest: grab a refresh token at https://developers.google.com/oauthplayground
#   Scopes: calendar + gmail.modify
# GOOGLE_REFRESH_TOKEN=
# GOOGLE_ACCESS_TOKEN=

# Notion: internal integration token
# https://www.notion.so/profile/integrations → New integration → Internal
# Share each page you want the agent to see with your integration.
# NOTION_TOKEN=

# Slack: bot token (start with this — needs search? add a user token too)
# https://api.slack.com/apps → Create → OAuth scopes → Install → Bot User OAuth Token
# SLACK_BOT_TOKEN=
# SLACK_USER_TOKEN=
